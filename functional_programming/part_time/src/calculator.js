
const _add = (a,b) => a+b;
const _sub = (a,b) => a-b;
const _mul = (a,b) => a*b;
const _div = (a,b) => a/b;
const _sum = (array = []) => {return 0.0;}

module.exports = {
    add : _add,
    sub : _sub,
    mul : _mul,
    div : _div,
    sum : _sum
}
